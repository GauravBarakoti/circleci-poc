# version: 2.1
# jobs:
  # test:
  #   docker:
  #     - image: cimg/python:3.10
  #   steps:
  #     - checkout
      # - run: python3 Flask_application.py
# workflows:
#   first:
#     jobs:
#       - test
version: 2.1
jobs:
  build:
    docker:
      - image: cimg/python:3.10
    steps:
      - checkout
      - run:
          name: Install Flask
          run: pip install Flask
  test:
    docker:
      - image: cimg/python:3.10
    steps:
      - checkout
      - run: python Flask_application.py


workflows:
  version: 2
  build-deploy:
    jobs:
      - build
      - test:
          requires:
            - build

